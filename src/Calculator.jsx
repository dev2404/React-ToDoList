function add(a, b) {
  let sum = a + b;
  return sum;
}

function divide(a, b) {
  let div = a / b;
  return div;
}

function subtract(a, b) {
  let sub = a - b;
  return sub;
}

function multiply(a, b) {
  let mul = a * b;
  return mul;
}

export { add, divide, subtract, multiply };
